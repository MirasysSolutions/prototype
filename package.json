{
  "name": "cloud-backend",
  "version": "1.0.0-alpha.0",
  "workspaces": [
    "services/*",
    "packages/*",
    "."
  ],
  "scripts": {
    "release": "lerna version --force-publish  --conventional-commits --no-push"
  },
  "devDependencies": {
    "@types/node": "^20.10.8",
    "lerna": "^8.0.2",
    "typescript": "^5.3.3"
  }
}
